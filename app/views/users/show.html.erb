<div class="user-profile__user-info">
  <%= @user.name %>
  <% if @current_user == @user %>
    <ul class="user-profile__controls">
      <li class="user-profile__controls--link">
        <%= link_to "Edit User", edit_user_path(@user.id) %>
      </li>
      <li class="user-profile__controls--link">
        <%= link_to "Delete User", user_path(@user.id), method: :delete, data: { confirm: "Are you sure?" } %>
      </li>
    </ul>
  <% end %>
</div>
<ul class="user-profile__puppies-list">
  <% @user.puppies.each do |puppy| %>
    <li class="user-profile__puppies-list--puppy"><%= link_to puppy.name, puppy_path(puppy.id), class: "user-profile__puppies-list--puppy-link" %></li>
  <% end %>
</ul>

